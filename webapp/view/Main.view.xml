<mvc:View controllerName="hds.ui5.todolistadvance.controller.Main"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    xmlns="sap.m">
    <Page id="page" title="{i18n>titleMainView}">
        <content>
            <HBox id="inputHbox">
                <Button press="openAddTaskDialog" icon="sap-icon://add" id="idBtnOpenTaskDialog" text="Add Task!" type="Default" width="200px" class="sapUi-Std-PaddingL"></Button>
            </HBox>
                <List
                    id="taskList_pending"
                    includeItemInSelection="true"
                    mode="None"
                    visible="{= ${/tasks}.length > 0 }"
                    items="{
                        path : '/tasks'
                        
                    }">
                    <CustomListItem id="customListItem">
                        <HBox id="hbox_custLstItm" justifyContent="SpaceBetween" alignItems="Center">
                            <!-- <Text id="idTaskTitle" text="{title}" class="sapUiSmallMarginBegin"/> -->
                            <!-- class="{= ${completed} ? 'strikeThrough' : '' }" />/ -->

                            <Text id="idTaskTitle" text="{title}" class="taskCompleted"/>
                            <HBox id="alighment" justifyContent="End" alignItems="Center">
                                <Text id="idDueDate" text="{dueDate}" class="hoverActions sapUiSmallMarginEnd"/>
                                <Text id="idCategory" text="{category}" class="hoverActions sapUiSmallMarginEnd"/>
                                <Text id="idPriority" text="{priority}" class="hoverActions sapUiSmallMarginEnd"/>
                                <HBox id="taskActions" class="hoverActions">
                                    <Button id="btnDelete" press="deleteTaskHandler" text="" icon="sap-icon://delete" class=" sapUiSmallMarginTopBottom"/>
                                    <Button id="btnEdit" enabled="{= ${Status} !== 'Completed' }" press="editTaskHandler" text="" icon="sap-icon://edit" class="sapUiSmallMarginBegin sapUiSmallMarginTopBottom"/>
                                    <Button enabled="{= ${Status} !== 'Completed' }" id="btnComplete" press="markCompleteteTaskHandler" text="" icon="sap-icon://complete" class="sapUiSmallMarginBegin sapUiSmallMarginTopBottom sapUiSmallMarginEnd" />
                                </HBox> 
                            </HBox>
                        </HBox>
                    </CustomListItem>
                </List>
                <List id="taskList_completed">
                    <items>
                        <StandardListItem id="todolistItemCompleted" title="{title}"/>
                    </items>
                </List>
             </content>
        <footer>
            <Toolbar id="toolbarFooter">
                <Text
                    id="footertext"
                    text="Developed @ Hikmah Dev Studio by Shahabuddin Ansari" />
            </Toolbar>
        </footer>
    </Page>
</mvc:View>